import React from "react"; 
import {Text, View} from "react-native"

const Chat = ({navigation}) => {
    return (
        <View>
            <Text>
                This is chat screen
            </Text>
        </View>        
    );
}

export default Chat;